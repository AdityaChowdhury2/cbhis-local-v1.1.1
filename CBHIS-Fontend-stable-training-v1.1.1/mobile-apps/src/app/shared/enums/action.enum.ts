export enum ActionState {
  INSERT = 1,
  GET = 2,
  UPDATE = 3,
  DELETE = 4,
}

export enum OnlineActions {
  CREATE = 'Create',
  READ = 'Read',
  UPDATE = 'Update',
  DELETE = 'Delete',
}
